@model IEnumerable<ProyectoPersonal.Models.Cotizador.UnidadMedida>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreUnidadMedida)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SiglaUnidad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreUnidadMedida)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SiglaUnidad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdUnidadMedida }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdUnidadMedida }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdUnidadMedida })
        </td>
    </tr>
}

</table>
