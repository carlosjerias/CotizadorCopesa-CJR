@model IEnumerable<ProyectoPersonal.Models.Cotizador.Encuadernacion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoMoneda.NombreTipoMoneda)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombreEncuadernacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ValorFijo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ValorVariable)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoMoneda.NombreTipoMoneda)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreEncuadernacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ValorFijo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ValorVariable)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdEncuadernacion }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdEncuadernacion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdEncuadernacion })
        </td>
    </tr>
}

</table>
