@model IEnumerable<SolicitudCotizador.Models.Encuadernacion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreEncuadernacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreEncuadernacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdEncuadernacion }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdEncuadernacion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdEncuadernacion })
        </td>
    </tr>
}

</table>
